import styled from '@emotion/styled'

export default styled.li`
  text-align: left;
  line-height: 1.5;
  font-size:1.2rem;
  margin:0 20px;
  padding:4px;
  list-style:none;
  @media(max-width:800px){
    font-size:1rem;
  }
`
